***************
*** 124,136 ****
 				if (
                     (!empty($data['manufacturers']) && !in_array($mid, $data['manufacturers'])) 
                     || (empty($data['include_margin_100']) && $group['profit_margin'] == 100) 
-                    || (!in_array($group['cb_status'], array('P','R')) 
                     && !in_array($group['dc_status'], array('C','S')))
                 ) {
 					unset($orders[$k]["shipping_groups"][$mid]);
 				} else {
 					$manufacturers[$mid] = $group["code"];
-                    $accounting_enabled = in_array($group['cb_status'], array('P','R')) 
                         && in_array($group['dc_status'], array('C','S'));
 					foreach ($price_details_names as $dn) {
 						if ($accounting_enabled) {
--- 124,136 ----
 				if (
                     (!empty($data['manufacturers']) && !in_array($mid, $data['manufacturers'])) 
                     || (empty($data['include_margin_100']) && $group['profit_margin'] == 100) 
+                    || (!in_array($group['cb_status'], array('P','R','H')) 
                     && !in_array($group['dc_status'], array('C','S')))
                 ) {
 					unset($orders[$k]["shipping_groups"][$mid]);
 				} else {
 					$manufacturers[$mid] = $group["code"];
+                    $accounting_enabled = in_array($group['cb_status'], array('P','R','H')) 
                         && in_array($group['dc_status'], array('C','S'));
 					foreach ($price_details_names as $dn) {
 						if ($accounting_enabled) {
