{* $Id: currency.tpl,v 1.13.2.1 2006/04/29 06:36:56 max Exp $ *}
{if $plain_text_message eq ""}<span style="WHITE-SPACE: nowrap">{/if}{if $display_sign}{if $value gte 0}+{else}-{/if}{/if}{$config.General.currency_symbol}{if (($price_type eq "product_price" and (($product.mult_order_quantity ne "Y") or ($product.mult_order_quantity eq "Y" and $product.product_availability eq "out of stock"))) or ($price_type eq "product_subtotal_value" and ( ($product.mult_order_quantity eq "Y" and $product.product_availability eq "in stock"))))}<span id="so_o_price" itemprop="price">{/if}{$value|abs_value|formatprice}{if (($price_type eq "product_price" and (($product.mult_order_quantity ne "Y") or ($product.mult_order_quantity eq "Y" and $product.product_availability eq "out of stock"))) or ($price_type eq "product_subtotal_value" and ( ($product.mult_order_quantity eq "Y" and $product.product_availability eq "in stock"))))}</span> {/if}{if $plain_text_message eq ""}</span>{/if}

