server {
    listen 80;

    server_name secure.s3stores.com;

    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl http2;
    server_name secure.s3stores.com;

    include configs/ssl.conf;

    access_log  /var/log/nginx/secure.s3stores.com.access.log;
    error_log   /var/log/nginx/secure.s3stores.error.log warn;

    location ~ / {
        proxy_pass https://secure_s3stores;
        proxy_redirect off;
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Real-IP $remote_addr;
    }
}